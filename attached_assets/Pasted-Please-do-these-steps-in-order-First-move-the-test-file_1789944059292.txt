Please do these steps in order. First, move the test file artifacts/bonsai-journal/src/tests/calendarEngine.test.ts into the existing folder that already contains ProgressionGallery.test.tsx and setup.ts (use git mv), and delete the now-empty tests folder. Do not touch the attached_assets folder.

Second, build a Care Calendar page. Create artifacts/bonsai-journal/src/pages/CalendarPage.tsx. It must use only the existing files calendarEngine.ts (generateCalendar) in src/lib and the two JSON files zones.json and groups.json in src/data/careCalendar. Do not edit those files. No backend, no API calls, no database changes, and no OpenAPI changes. Use the project's existing shadcn components (Select, Checkbox, Switch, Card, Badge, Alert) and theme colors so it works in light and dark mode. Do not hard-code page-wide hex colors.

The page has a title "Care Calendar" and one line of intro text saying these are typical timings for your climate zone and that dates are approximate, so always go by what the tree is actually doing. Step 1 is a zone dropdown listing every zone's display text, defaulting to zone 5b. Step 2 is a checkbox for each of the five groups showing its icon, label, and its examples as small helper text, with Hardy Deciduous ticked by default. Add a Switch labelled "Show advanced tasks" that is off by default and passes includeOptional to generateCalendar. There is no Generate button: the calendar updates live whenever the zone, groups, or switch change. Remember the last zone, ticked groups, and switch state in localStorage under the key bonsai:careCalendar, with all reads and writes inside try/catch, and fall back to the defaults if anything is missing or invalid.

Under the controls, if generateCalendar returns any zoneNotes, show them in an Alert with the group label and its note text. Then show the 12 months as stacked cards, one column, each with the month name. Mark the current calendar month with a small "This month" badge. A month with no tasks shows a muted "Nothing scheduled". Each task shows the group icon, the title, a Badge with its care_type, its timeLabel (for example "early May"), the desc, the warning in red text if it is not empty, and the trigger as small italic text. Give each group a subtle left border or tinted background so groups are visually distinct without relying on color alone (the icon and group label already help): hardy deciduous green, two-flush pines blue, one-flush pines teal, junipers olive or emerald, tropical amber.

Third, add the route. In App.tsx add a /calendar route that follows exactly the same pattern as the existing /stats route (Protected wrapping Layout). In Navbar.tsx add a "Care Calendar" link right after Stats, styled the same way. If the app has a mobile navigation for Stats, add the same there. If it does not, tell me and do not invent one.

Finally verify. Run the calendarEngine test file again and paste the full real output (13 tests should pass). Run the bonsai-journal typecheck and paste the real output, and tell me whether any error is new compared to the two known ones in calendar.tsx and spinner.tsx. Tell me honestly whether you actually loaded the /calendar page in a browser. If you did not, say you did not. Do not claim visual checks you did not do. Then confirm you are on the care-calendar branch, commit locally with the message "Add Care Calendar page, route and nav link", and report the full commit hash and the output of git show --stat. Do not push, publish, rebase, merge, or modify anything on the remote GitHub repository. Commit locally only. I will push manually.
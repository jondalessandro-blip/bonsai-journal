In CollectionPage.tsx, continue the multi-select filter work from before. The state was already renamed to climateFilter, foliageFilter, stageFilter, and statusFilter (each a FilterState with values and mode), but queryParams, navFilterQuery, and the UI still reference the old variable names. Fix all of that now.

Step 1: Replace the queryParams useMemo so that for each of climate, foliage, stage, and status, it sends the plain field name when that filter's mode is "include" and has values, or the field name plus "Exclude" when the mode is "exclude" and has values, otherwise undefined. For example: climate should be climateFilter.mode === "include" && climateFilter.values.length > 0 ? climateFilter.values : undefined, and climateExclude should be climateFilter.mode === "exclude" && climateFilter.values.length > 0 ? climateFilter.values : undefined. Apply the same pattern for foliage and foliageExclude, stage and stageExclude, status and statusExclude. Update the dependency array to use climateFilter, foliageFilter, stageFilter, statusFilter instead of climate, foliage, stage, status.

Step 2: Replace the navFilterQuery useMemo with the same applyFilter-based approach used in the earlier URL-sync useEffect: build a URLSearchParams from debouncedSearch, then for each of the four FilterState objects, if values.length is greater than zero, set the param named field (or field plus "Exclude" if mode is "exclude") to values.join(","), then include tags if selectedTags is non-empty, and return params.toString(). Update the dependency array to use climateFilter, foliageFilter, stageFilter, statusFilter instead of climate, foliage, stage, status.

Step 3: In the collapsible filter panel, replace the four Select-based dropdowns for climate, foliage, stage, and status with popovers matching the structure of the existing tags popover already in this file (Popover, PopoverTrigger with a Button showing the filter name and a count badge when active, PopoverContent with a checkbox-style option list). Each popover's content should include, above the option list, a small two-button Include and Exclude mode toggle that updates that filter's mode field, and the option list should call toggleFilterValue to add or remove values. Use CLIMATE_OPTIONS with CLIMATE_LABELS for the climate popover, FOLIAGE_OPTIONS with FOLIAGE_LABELS for foliage, STAGE_OPTIONS with STAGE_ITEM_LABELS for stage (so the numbered prefixes still show), and STATUS_OPTIONS with STATUS_LABELS for status. When a filter's mode is exclude, the trigger button's label should show "Not: " followed by the filter name instead of just the filter name. When no values are selected, show "All Climates" / "All Foliage" / "All Stages" / "All Statuses" same as before.

Step 4: Replace the four active-filter chip blocks for climate, foliage, stage, and status (the ones currently checking a single value not equal to "all") with a version that renders one chip per selected value in that filter's values array, using each field's own LABELS map for the chip text, prefixed with "Not " when that filter's mode is "exclude", with an X button that removes just that one value via toggleFilterValue.

Step 5: In TreeDetailPage.tsx, replace the navFilters block (built from useSearch and URLSearchParams, currently returning search, climate, foliage, stage, status, and tags as single values or a plain array for tags) with a version that parses climate, climateExclude, foliage, foliageExclude, stage, stageExclude, status, statusExclude, and tags all as comma-split arrays, or undefined if that URL param is absent, using a small local helper that reads a URLSearchParams key and splits it on commas if present.

Do not change any other logic, components, or files. After making these changes, commit on the dev branch, no need to push since I will push manually, then run git log -1 and paste that exact output.
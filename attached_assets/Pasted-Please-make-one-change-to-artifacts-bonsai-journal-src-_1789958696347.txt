Please make one change to artifacts/bonsai-journal/src/components/Navbar.tsx and nothing else. First check that the branch is dev and that git status shows no changes to tracked files (untracked files inside attached_assets are fine and can be ignored). If not, stop and tell me.

On phone-sized screens the top bar hides its links (Home, Stats, Care Calendar), so phone users cannot reach Stats or the Care Calendar. Add a phone-only menu. It must only be visible below the md breakpoint (use md:hidden) and only when the user is signed in (inside the same signed-in Show wrapper the existing links use). Place it inside the left part of the nav bar, immediately before the logo link. It is a ghost icon Button (at least about 40 pixels square so it is easy to tap) showing the Menu icon from lucide-react, with an accessible label "Open menu". Use the project's existing DropdownMenu components from ui/dropdown-menu. The menu has three items, each a real link: the first goes to "/" and uses exactly the same visible label as the existing desktop link to "/", the second is "Stats" going to /stats, and the third is "Care Calendar" going to /calendar. Use the same wouter link pattern as the existing links (for example with asChild). The item for the page the user is currently on should look highlighted and have aria-current set to page.

Do not change the desktop links, the Sign out button, the logo, any styles for larger screens, or any other file. Do not add any new dependency, and do not touch the database, the API server, or the OpenAPI spec.

Then verify with real output. Run the full vitest suite for the bonsai-journal app (not only the calendar test), paste the full unedited output, and tell me the total number of tests passed and confirm that none failed. Run the bonsai-journal typecheck and tell me whether any error is new compared to the two known ones in calendar.tsx and spinner.tsx. Show me the git diff so I can confirm only Navbar.tsx changed. Tell me honestly whether you loaded the page in a browser at phone width, and if you did not, say so. Commit locally on the dev branch with the message "Add phone menu for Stats and Care Calendar" and report the full commit hash and the output of git show --stat. Do not add anything from attached_assets to the commit. Do not push, publish, rebase, merge, or modify anything on the remote GitHub repository. Commit locally only. I will push manually.
> Please fix a time zone bug in the upcoming reminders feature. Right now the server works out "today" using its own clock, which is UTC, so between roughly 8pm and midnight Eastern the app thinks it is already tomorrow. For example, a reminder due Sep 20 shows as "Today" at 9:49pm ET on Sep 19. The fix is to have the app send the user's own local date to the server. Please do these steps in this order.
>
> Step 1, API spec. In lib/api-spec/openapi.yaml, add an optional query parameter called today to the GET /reminders/upcoming operation. It is a string in yyyy-MM-dd format, with a pattern that only allows that shape. Then run pnpm --filter @workspace/api-spec run codegen. Do not hand-edit anything under lib/api-zod or lib/api-client-react.
>
> Step 2, backend. In artifacts/api-server/src/routes/trees.ts, in the /reminders/upcoming handler, use the today parameter when it is provided and valid. If it is missing or invalid, fall back to the current behaviour so nothing breaks for older clients. Compute the 30 day window from that date string. Compute daysUntilDue by turning both the due date and the today string into UTC midnight dates from their yyyy-MM-dd text and dividing the difference by 86400000, so the result is always a whole number with no Math.ceil and no setHours. Same-day must come out as exactly 0 and the next day as exactly 1. Leave the other UTC date defaults in that file alone for now.
>
> Step 3, web frontend. Find wherever artifacts/bonsai-journal calls the upcoming reminders hook and pass today as format(new Date(), "yyyy-MM-dd") using date-fns, which uses the browser's local time. Make sure the value is part of the query so it refreshes correctly. Also fix the UTC default dates in artifacts/bonsai-journal/src/components/LogForm.tsx (around line 58), artifacts/bonsai-journal/src/components/BulkLogForm.tsx (around line 44), and artifacts/bonsai-journal/src/components/ProgressionGallery.tsx (around line 92) by replacing new Date().toISOString() date slicing with format(new Date(), "yyyy-MM-dd"), so the default date on new logs and photos is the user's local date in the evening too.
>
> Step 4, mobile app. In artifacts/bonsai-mobile, pass the same local today value where the reminders screen calls the upcoming reminders hook, and replace the toISOString().slice(0, 10) defaults in app/add-log.tsx and app/add-photo.tsx with the device's local date in yyyy-MM-dd form.
>
> Please make no other changes. Run the typecheck and tell me whether anything new fails beyond the known pre-existing failures in calendar.tsx, spinner.tsx, and TreeDetailPage.tsx. Restart the API Server workspace so the new build is picked up. To verify, call the upcoming reminders endpoint with today set to 2026-09-19 and show me the real response for a reminder due 2026-09-20, which should show daysUntilDue of 1, and then again with today set to 2026-09-20, which should show 0. Paste the actual output, not a description of it.
>
> Commit locally only and report the real commit hash. Do not push, publish, rebase, or modify anything on the remote GitHub branch. I will push manually.
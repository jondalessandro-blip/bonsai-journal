Now I want to replace the two placeholder dialogs on the Collection page with real forms that use the bulk care endpoints we already built (createBulkTreeLogs and createBulkTreeReminders, available as the generated hooks useCreateBulkTreeLogs and useCreateBulkTreeReminders from @workspace/api-client-react).

Create a new component BulkLogForm.tsx, modeled loosely on the existing LogForm.tsx but simpler, since bulk actions are always creating new entries, never editing. Props: treeIds (string array), onSuccess (a function with no arguments). The form has: a care event type field using the existing CareEventMultiSelect component so the user can pick one or more event types (e.g. Watering, Fertilizing), a date field defaulting to today, and an optional notes textarea, matching the look and field labels of LogForm. On submit, for each selected type, call useCreateBulkTreeLogs's mutateAsync with data containing treeIds, that type, the date, and notes (omit notes if empty). Run these calls with Promise.all. On success, invalidate the relevant queries for every tree id in treeIds (the same three query keys LogForm invalidates today: ["/api/trees", treeId, "logs"], ["/api/trees", treeId, "timeline"], ["/api/trees", treeId, "reminders"]) plus ["/api/reminders/upcoming"] once, then call onSuccess. Show an inline error message in the same style as LogForm if any of the calls fail, without closing the dialog, so the user can retry.

Create a second new component BulkReminderForm.tsx the same way, modeled on ReminderForm.tsx: props treeIds and onSuccess, fields for care event type(s) via CareEventMultiSelect, a due date field defaulting to one week from today, and optional notes. On submit, call useCreateBulkTreeReminders once per selected type with treeIds, that type, dueDate, and notes. Same invalidation and error-handling approach as above.

In CollectionPage.tsx, replace the placeholder content in the two Dialogs from the previous step with these real forms, passing treeIds={Array.from(selectedTreeIds)} and an onSuccess that closes the dialog, clears selectedTreeIds, and turns off selectMode. Also show the count of trees being affected somewhere near the top of each dialog, like "Applying to 3 trees", so it's clear what the action will affect before the user submits.

Just make the changes and confirm what you edited, including which files changed. Don't push to GitHub — I'll do that myself.
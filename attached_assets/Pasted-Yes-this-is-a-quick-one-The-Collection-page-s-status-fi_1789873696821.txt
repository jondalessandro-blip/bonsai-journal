Yes, this is a quick one. The Collection page's status filter has its own hard-coded list (`STATUS_OPTIONS` and `STATUS_LABELS` in `CollectionPage.tsx`) that was never updated when "Healthy" was added to `TreeForm.tsx`. The backend filter accepts any status text, so nothing needs to change there or in the API spec.

Three other places also don't know about "Healthy" yet, so trees with that status show a plain grey badge: `TreeCard.tsx`, `TreeGridTile.tsx` and `TreeDetailPage.tsx`. On the Stats page it's tacked on at the end of the list rather than sitting next to Thriving. I bundled those into the same prompt, using teal so it's distinguishable from Thriving's green. Tell me if you'd rather a different colour.

Your sample trees are also seeded with "Healthy", so this will fix those too.

> In artifacts/bonsai-journal, add the "Healthy" health status everywhere the status list is hard-coded, so it matches the pick list in TreeForm.tsx (order: Thriving, Healthy, Dormant, Stressed/In Distress, Sick, Dead/Beyond Recovery). This is frontend only: do not touch the API server, the OpenAPI spec, or any generated files. First, in src/pages/CollectionPage.tsx, add "Healthy" to STATUS_OPTIONS and to STATUS_LABELS, placed right after "Thriving", so it appears in the Status filter dropdown and displays correctly on the active filter chips, including Include and Exclude modes. Second, give Healthy its own badge colour, teal, using the classes bg-teal-100 text-teal-800 dark:bg-teal-900/40 dark:text-teal-300, in the status badge colour logic in src/components/TreeCard.tsx, src/components/TreeGridTile.tsx and src/pages/TreeDetailPage.tsx. In TreeDetailPage.tsx also add "Healthy" to the array used for the grey fallback check so it does not get the fallback styling. Third, in src/pages/StatsPage.tsx, add "Healthy" to STATUS_ORDER right after "Thriving", add a teal entry to STATUS_COLORS (#0d9488), and add the same teal classes to STATUS_BG. Do not change any other status values or any other behaviour. When finished, run the typecheck and confirm you introduced no new errors (the existing known failures in calendar.tsx, spinner.tsx and TreeDetailPage.tsx line 48 are not related). Then confirm in the running app that "Healthy" appears in the Status filter on the Collection page, that selecting it shows only Healthy trees, and that Healthy badges are teal on cards, grid tiles and the tree detail page. Commit locally only, report the real commit hash, and do not push, publish, rebase or modify anything on the remote GitHub branch. I will push manually.

Once you've pushed, send me the commit hash and I'll check the actual diff on `origin/dev` as usual.
Please make the following changes and nothing else. All paths are inside artifacts/bonsai-journal/src.

Change 1, data file. In data/careCalendar/groups.json make these edits and do not reformat or change anything else in the file. In the group labelled "Tropical, Subtropical & Tender Succulents", find the task titled "Repot" and add one new property named latest whose value is an object with month set to 7 and part set to "mid". Then find the four tasks titled "Winter moisture check", one in each of these groups: "Hardy Deciduous", "Two-Flush Pines", "Pines & Spruce (One Flush)", and "Junipers & Cedars". In all four, set the desc property to exactly this text: Check that the soil in stored pots hasn't dried out completely. Evergreens keep losing water through winter, so they need this most. Water lightly on a day above freezing if the soil is dry. And set the warning property in all four to exactly this text: Dry pots are a very common cause of winter losses. Don't overdo it, since soggy soil that freezes is also bad. The trigger, timing, care type and everything else on those four tasks stay as they are.

Change 2, engine. In lib/calendarEngine.ts add one new exported type and one new exported function, and do not change any existing code in that file. The type is called MergedCalendarTask: an object with a task property holding a CalendarTask, and a groups property holding an array of objects that each have group_id, group_label and group_icon. The function is called mergeIdenticalTasks. It takes an array of CalendarTask and returns an array of MergedCalendarTask. Two tasks are treated as identical when their title, care_type, timeLabel, desc, warning and trigger are all exactly equal. Identical tasks become one entry that keeps the first task and lists every group it came from. Keep the original order of first appearance.

Change 3, page. In pages/CalendarPage.tsx, make the month cards use mergeIdenticalTasks on each month's tasks. When a merged entry has more than one group, show every group's icon at the left of the card, add a small muted line "Applies to:" followed by the group labels, and use the neutral border style instead of any single group's color. When it has one group, it looks exactly as it does now. Also, when no tree groups are ticked, replace the 12 month cards with one short muted message: "Tick at least one tree group above to see your calendar." Do not change anything else on the page.

Change 4, tests. In the existing test file calendarEngine.test.ts add these tests. First, with zone 3a the tropical group's Repot task lands in month 7, and with zone 5b it still lands in month 6. Second, with zone 5b and all five groups selected and advanced tasks off, the merged January task list contains exactly one task titled "Winter moisture check", and that entry lists four groups.

Then verify with real output. Run the calendarEngine test file and paste the full unedited output. 15 tests should pass. If any fail, paste the failure and do not edit existing tests to force a pass. Run the bonsai-journal typecheck and tell me whether any error is new compared to the two known ones in calendar.tsx and spinner.tsx. Show me the git diff of groups.json so I can confirm only the intended lines changed. Tell me honestly whether you actually loaded the page in a browser, and if you did not, say so. Confirm you are on the care-calendar branch, commit locally with the message "Cold-zone tropical repot cap, merge duplicate tasks, add empty state", and report the full commit hash and the output of git show --stat. Do not push, publish, rebase, merge, or modify anything on the remote GitHub repository. Commit locally only. I will push manually.
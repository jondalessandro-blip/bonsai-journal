In the bonsai-journal frontend, I want pilot users to be able to choose which existing progression photo is used as the hero/cover image for a tree, instead of it always being the most recently uploaded photo. Make these changes only, no backend or schema changes needed since the tree already has photoUrl, coverThumb, and coverPosition fields. First, in CoverPhotoHero.tsx, add a new optional prop called serverPhotoUrl of type string or null or undefined. Change the coverPhoto derivation so that instead of always picking photos[photos.length - 1], it first tries to find the photo in the photos array whose photoUrl matches serverPhotoUrl, and only falls back to the most recent photo if no match is found or serverPhotoUrl is not provided. Second, in TreeDetailPage.tsx, find where CoverPhotoHero is rendered and pass an additional prop serverPhotoUrl={tree.photoUrl}. Third, in ProgressionGallery.tsx, add a small "Set as cover" button to each photo tile in the gallery grid, positioned near the existing delete button, using a star or image icon from lucide-react. When clicked, it should call updateTree.mutate with the tree id and data containing photoUrl set to that photo's photoUrl, coverThumb set to that photo's photoThumb if it has one, and coverPosition reset to a fresh copy of DEFAULT_COVER_POSITION, exactly matching the pattern already used in handleFileChange's auto-promote logic including clearing the localStorage cover position entry for that tree id using coverPositionStorageKey, and on success invalidate both the ["/api/trees", treeId] query key and the ["/api/trees"] query key so the detail hero and collection thumbnail both refresh. Also visually indicate which photo is currently the cover by comparing each photo's photoUrl against tree.photoUrl and showing a small filled star badge or highlighted border on that tile; you'll need to accept tree as a prop in ProgressionGallery or pass tree.photoUrl down alongside treeId for this comparison. Do not change the automatic promote-to-cover behavior on new photo upload — that should keep working as it does now.
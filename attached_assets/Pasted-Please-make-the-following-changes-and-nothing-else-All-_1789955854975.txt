Please make the following changes and nothing else. All paths are inside artifacts/bonsai-journal/src. Do not recreate or change mergeIdenticalTasks, MergedCalendarTask, or the empty-state message, which already exist.

Change 1, data file. In data/careCalendar/groups.json add one new property named short_label to each of the five groups and change nothing else in the file: "Hardy Deciduous" gets "Hardy Deciduous", "Two-Flush Pines" gets "Two-Flush Pines", "Pines & Spruce (One Flush)" gets "Pines & Spruce", "Junipers & Cedars" gets "Junipers", and "Tropical, Subtropical & Tender Succulents" gets "Tropical".

Change 2, engine. In lib/calendarEngine.ts add a required string property short_label to the Group type. Add a string property group_short_label to the CalendarTask type, and fill it in generateCalendar from the group's short_label, next to the existing group_label. Do not change anything else in that file.

Change 3, page. In pages/CalendarPage.tsx change how each month card lists its tasks. Inside each month card, group the single-group tasks under small headings, one heading per tree group, written as real text in small uppercase muted letters with a little letter spacing, using the group's short_label. Order the headings in the same order the groups appear in groups.json, and keep tasks under a heading in date order. Tasks that were merged from more than one group go into a final section headed "Several groups" in the same heading style. Those merged cards stay exactly as they are now: every group's icon, the "Applies to:" line with the full group labels, and the neutral tint. Single-group cards keep their group tint, left border and icon. On every card, move the trigger text so it sits in small italic directly under the title row and above the description, and remove it from the bottom of the card. Keep the "Trigger:" wording as it is. Keep the care type badge, the time label, the description and the red warning as they are. A month with no tasks keeps its muted "Nothing scheduled". Do not change anything else on the page.

Change 4, tests. In the existing test file calendarEngine.test.ts add one test: every task returned by generateCalendar for the tropical group has group_short_label equal to "Tropical".

Then verify with real output. Run the calendarEngine test file and paste the full unedited output. 16 tests should pass. If any fail, paste the failure and do not edit existing tests to force a pass. Run the bonsai-journal typecheck and tell me whether any error is new compared to the two known ones in calendar.tsx and spinner.tsx. Show me the git diff of groups.json so I can confirm only the five short_label lines were added. Tell me honestly whether you actually loaded the page in a browser, and if you did not, say so. Confirm you are on the care-calendar branch, commit locally with the message "Add group headings and move trigger above description", and report the full commit hash and the output of git show --stat. Do not push, publish, rebase, merge, or modify anything on the remote GitHub repository. Commit locally only. I will push manually.